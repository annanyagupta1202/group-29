export type Objective = 'Premium Brand' | 'Balanced Growth' | 'Fast Payback';
export type Risk = 'Conservative' | 'Balanced' | 'Aggressive';
export type Scenario = 'Base' | 'Downside' | 'Upside';
export type Controls = { objective: Objective; risk: Risk; scenario: Scenario };
export type Provenance = 'Observed Data' | 'Derived Calculation' | 'Assumption' | 'Forecast';
export type PriceTest = { price:number; channel:string; acceptance:number; net:number; contribution:number; margin:number };
export type Segment = { name:string; n:number; frequency:number; spend:number; sensitivity:number; intent:number; channel:{dtc:number;retail:number;gym:number}; vw:number[] };
export type City = { name:string; n:number; intent:number; frequency:number; spend:number; marketShare:number; cagr:number };
export type ChannelMetric = { name:string; preference:number; cac:number; ltv:number; source:string };
export type Recommendation = { controls:Controls; price:number; priceRows:PriceTest[]; target:Segment; mix:{channel:string;share:number;contribution:number;cac:number;ltv:number}[]; cities:(City & {score:number})[]; launch:{month:string;index:number;reason:string}; cmo:number;cfo:number;overall:number; weights:{cmo:number;cfo:number}; options:{price:number;cmo:number;cfo:number;overall:number;selected:boolean}[]; acceptance:number; contribution:number; expectedContribution:number; contributionPath:{customers:number;frequency:number;weightedUnitContribution:number;demandFactor:number};cac:number;ltv:number;ratio:number;roi:number;paybackUnits:number; guardrailMet:boolean; guardrailWarning:string|null; position:string; why:string[]; sacrifice:string; confidence:string; assumptions:string[]; provenance:string[]; scenarios:{scenario:Scenario;acceptance:number;cac:number;ltv:number;ratio:number;contribution:number;expectedContribution:number;payback:number}[] };
export type StressCase = {name:string;assumption:string;baseline:string;stressed:string;impact:string;status:'Recommendation survives'|'Recommendation weakens'|'Recommendation changes';price:number;month:string;acceptance:number;expectedContribution:number;cac:number;ratio:number;payback:number};
export type StressReport = {baseline:Recommendation;cases:StressCase[];breakPoint:{stress:string;trigger:string;fallbackPrice:number;fallbackMonth:string;reason:string}|null;why:string};
