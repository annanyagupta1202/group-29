export function ErrorState({onRetry,onReset}:{onRetry:()=>void;onReset:()=>void}){return <main className="error-state" role="alert"><h1>We couldn't build this recommendation.</h1><p>The selected inputs are empty or inconsistent. Reset the configuration and try again.</p><div><button type="button" onClick={onRetry}>Reset and retry</button><button type="button" onClick={onReset}>Reset configuration</button></div></main>}
